extends ./layout.jade
block main
  header.page-header
    h1
      | Market Day
      small #{new Date().toLocaleDateString()}
  main#marketday
    #orders
      form.form#search
        .form-group
          .input-group
            span.input-group-addon: span.glyphicon.glyphicon-search
            input.form-control#filter-orders(type='search' placeholder='Search for a customer')
