| {{#orders}}
.panel.order.panel-default
  .panel-heading
    h4.panel-title
      a(data-toggle="collapse" href="order-item-{{ID}}") {{Customer}}
  .panel-collapse.collapse(id="order-item-{{ID}}")
    .panel-body
      table.table.order-items
        tbody
          | {{#items}}
          tr(id="{{domID}}")
            td.product {{Product}}
            td.quantity {{Quantity}}
            td.actions {{Actions}}
          | {{/items}}
| {{/orders}}
